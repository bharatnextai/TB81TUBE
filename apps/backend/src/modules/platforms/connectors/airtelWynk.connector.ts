import { Platform } from "@prisma/client";
import { PlaceholderPlatformConnector } from "./placeholderPlatform.connector.js";

export const airtelWynkConnector = new PlaceholderPlatformConnector(Platform.AIRTEL_WYNK);
